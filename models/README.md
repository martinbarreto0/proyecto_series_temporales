# Modelos entrenados

Los archivos de modelo `mejor_modelo_tetuan.joblib` y `mejor_modelo_tetuan.pkl` no se incluyen directamente en el repositorio porque superan el límite de tamaño permitido por GitHub..

Los modelos entrenados se encuentran disponibles en la sección **Releases** del repositorio, en la versión `v1.0`.